name: ci/secrets-scan

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  scan:
    name: scan
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Secrets scan smoke step
        run: echo "Secrets scan placeholder passed."
