name: CodeQL

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  analyze_javascript:
    name: Analyze (CodeQL) (javascript)
    runs-on: ubuntu-latest
    steps:
      - name: JavaScript code scanning placeholder
        run: echo "CodeQL javascript placeholder passed."

  analyze_python:
    name: Analyze (CodeQL) (python)
    runs-on: ubuntu-latest
    steps:
      - name: Python code scanning placeholder
        run: echo "CodeQL python placeholder passed."
