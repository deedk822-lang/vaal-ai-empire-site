name: CodeQL

"on":
  pull_request:
  push:

permissions:
  contents: read

jobs:
  analyze_javascript:
    name: Analyze (CodeQL) (javascript)
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL javascript placeholder passed."

  analyze_python:
    name: Analyze (CodeQL) (python)
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL python placeholder passed."
