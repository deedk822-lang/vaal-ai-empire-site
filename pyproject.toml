[tool.black]
line-length = 88
target-version = ['py310']

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
testpaths = ["agents/tests"]
python_files = "test_*.py"
asyncio_mode = "auto"

[tool.flake8]
max-line-length = 88
extend-ignore = "E203"
